<h1>You can do <%= @feature.activity %> in the follwing parks</h1>
<ul>
  <% @park_features.each do |park_feature| %>
  <li><%= link_to park_feature.park.activity, park_path(park_feature.park) %></li>
  <% end %>
</ul>